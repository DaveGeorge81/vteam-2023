checks:
    javascript: true
build:
    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run
        tests: true

filter:
    excluded_paths: [test/*]

tools:
    external_code_coverage: true